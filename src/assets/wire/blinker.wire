; Simple blinker - toggles LED on each clock
; Good for testing, demonstrates basic sequential logic

module simple_blinker(clk) -> led:
  ; Use nand(x, x) to implement NOT
  inverted = nand(led, led)
  led = dff(inverted, clk)
